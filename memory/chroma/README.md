# ChromaDB Runtime

Docker Compose starts ChromaDB as the vector store for future memory and RAG workloads.

Phase 1 only configures the runtime boundary. Future phases will add:

- Embedding pipelines.
- Tenant and workspace collection policies.
- Retrieval quality evaluation.
- Retention and deletion propagation for derived vectors.
